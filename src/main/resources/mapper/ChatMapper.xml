<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.stock.dashboard.dao.ChatDao">

    <insert id="insertMessage" parameterType="com.stock.dashboard.dto.ChatMessageDto">
        INSERT INTO CHAT_MESSAGE (TICKER, USER_EMAIL, NICKNAME, CONTENT, CREATED_AT)
        VALUES (#{ticker}, #{userEmail}, #{nickname}, #{content}, NOW())
    </insert>

    <select id="selectRecentMessages" resultType="com.stock.dashboard.dto.ChatMessageDto">
        SELECT MSG_ID, TICKER, USER_EMAIL, NICKNAME, CONTENT, CREATED_AT
        FROM CHAT_MESSAGE
        WHERE TICKER = #{ticker}
        ORDER BY CREATED_AT DESC
        LIMIT #{limit}
    </select>

</mapper>